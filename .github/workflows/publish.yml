name: publish
on:
  push:
    tags:
      - '*'
jobs:
  pre:
    name: pre
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Stop process if the branch isn't the master
        if: endsWith(github.event.base_ref, 'dfomin') == false
        run: exit 1
      - name: Install dependencies
        run: yarn install
      - run: ls -la
      - name: Cache project
        uses: actions/cache@v3
        with:
          path: ./*
          key: project-${{ github.ref_name }}-v1
  build:
    name: build
    needs: pre
    runs-on: ubuntu-latest
    steps:
      - name: Get project cache
        uses: actions/cache@v3
        with:
          path: ./*
          key: project-${{ github.ref_name }}-v1
      - run: ls -la
      - name: Build from source
        run: yarn build:prod
      - name: Cache project
        uses: actions/cache@v3
        with:
          path: ./*
          key: project-${{ github.ref_name }}-v2
  change-package:
    name: change-package
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Get project cache
        uses: actions/cache@v3
        with:
          path: ./*
          key: project-${{ github.ref_name }}-v2
      - name: Run change package script
        run: TAG_VERSION=${GITHUB_REF#refs/tags/} node ./scripts/change-package.js
      - name: Cache project
        uses: actions/cache@v3
        with:
          path: ./*
          key: project-${{ github.ref_name }}-v3
  publish-package:
    name: publish-package
    needs: change-package
    runs-on: ubuntu-latest
    steps:
      - name: Get project cache
        uses: actions/cache@v3
        with:
          path: ./*
          key: project-${{ github.ref_name }}-v3
      - name: Publish into npm
        run: |
          ls -la
          cat package.json
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}

        # yarn publish --access public